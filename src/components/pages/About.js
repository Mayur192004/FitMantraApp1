import React from 'react'
import Navbar from '../Navbar'
export default function About({isLoggedin} ) {
  return (
    <div>
      <Navbar isLoggedin={isLoggedin} ></Navbar>
      {/* Will be doing it in future Scope */}
      Future Implementation
    
    </div>
  )
}
